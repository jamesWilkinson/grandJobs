/**
	GRAND JOBS - GAME ENGINE API
	USED AS THE CONNECTION LAYER BETWEEN THE GAMEMODE AND THE FILTERSCRIPT
**/


// META
//forward SetGameAuthor(author[]);
//forward GetGameAuthor();
//forward SetGameName(name[]);
//forward GetGameName();
//forward SetFileName(filename[]);
//forward GetFilename();
//forward SetVersionMinor(ver);
//forward GetVersionMinor();
//forward SetVersionMajor(ver);
//forward GetVersionMajor();

//LOADER
//forward GetNumberOfGames();

// SCREENFADE
//forward UnfadePlayerScreen(playerid);

